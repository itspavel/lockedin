## What & why

<!-- What does this change, and what problem does it solve? -->

## Checklist

- [ ] `swift build` passes
- [ ] No message/prompt/code *content* is read from Claude Code logs (privacy contract)
- [ ] UI uses Theme tokens + SF Symbols (no emoji)
- [ ] Screenshots attached for UI changes (`swift run LockedIn --render /tmp/preview`)
